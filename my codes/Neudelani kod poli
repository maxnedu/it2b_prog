# include <stdio.h>

int main(){
    int pole[5];
    for(int i = 0; i < 6; i++ ){
    printf("Zadej cislo %d:", i + 1);
    scanf("%d", &pole[i]);
    }
    for(int j = 0; j < 6; j++){
        if(pole[j] = )
    }
   
        
    
    printf("Ano, spravne");
    return 0;
}